# Tools

This directory contains utilities used to validate, maintain and support the
Quantum Computing Lab repository.

Tools should not contain core educational content or reusable library code.
Reusable Python functionality belongs under `src/`.
